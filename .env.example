# ===================================================
# Trump-Scan API Environment Configuration
# ===================================================
# 이 파일을 .env로 복사하고 실제 값을 입력하세요.
# .env 파일은 Git에 커밋되지 않습니다 (민감 정보 포함)
#
# 사용법:
#   cp .env.example .env
#   # .env 파일을 편집하여 실제 값 입력
# ===================================================

# ===================================================
# Application Settings
# ===================================================
# 서버 포트 (기본값: 3000)
PORT=3000

# 로그 레벨: debug, info, warn, error (기본값: info)
LOG_LEVEL=info

# CORS 허용 도메인 (기본값: * 전체 허용)
CORS_ORIGIN=*

# ===================================================
# Oracle Database (Required)
# ===================================================
# Oracle DB 사용자명
DB_USERNAME=your_username

# Oracle DB 비밀번호
DB_PASSWORD=your_password

# Oracle DB 연결 문자열 (tnsnames.ora 참조)
DB_DSN=your_dsn

# Oracle Wallet 디렉토리 경로
# 로컬 개발: /path/to/your/wallet
# Docker: /opt/oracle/wallet (런타임에 마운트)
DB_WALLET_LOCATION=/path/to/wallet

# Oracle Wallet 비밀번호
DB_WALLET_PASSWORD=your_wallet_password

# ===================================================
# Redis Settings
# ===================================================
# Redis 호스트 (기본값: localhost)
# Docker에서 호스트 Redis 접근: host.docker.internal
REDIS_HOST=localhost

# Redis 포트 (기본값: 6379)
REDIS_PORT=6379

# Redis 데이터베이스 번호 0-15 (기본값: 0)
REDIS_DB=0

# Redis 비밀번호 (선택사항, 없으면 비워두세요)
REDIS_PASSWORD=

# ===================================================
# Redis Streams Configuration
# ===================================================
# 피드 생성 이벤트 입력 스트림
REDIS_INPUT_STREAM=trump-scan:feed-generation:new-feed

# Consumer Group 이름
REDIS_CONSUMER_GROUP=api-notifiers

# Consumer 이름 (워커 인스턴스별 고유)
REDIS_CONSUMER_NAME=api-worker-1

# 블록 타임아웃 (밀리초, 기본값: 5000)
REDIS_BLOCK_TIMEOUT=5000
